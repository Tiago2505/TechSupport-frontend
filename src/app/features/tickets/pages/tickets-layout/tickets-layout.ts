import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-tickets-layout',
  imports: [RouterOutlet],
  templateUrl: './tickets-layout.html',
})
export class TicketsLayout {}
